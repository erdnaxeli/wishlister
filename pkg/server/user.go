package server

/*
func (s Server) setUserSessionCookie(c echo.Context, userID string) {
	cookie := &http.Cookie{
		Name:     "session_id",
		Value:    userID,
		Secure:   true,
		HttpOnly: true,
	}

	c.SetCookie(cookie)
}
*/
